name: Format

on:
  push:
  pull_request:
    types: [opened, reopened, synchronize]

jobs:
  black:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ${{ toJson(vars.PYTHON_VERSIONS) }}

    name: Black on ${{ matrix.python-version }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up CPython ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        id: install-deps
        run: |
          pip install .[tests]
          pip install black

      - name: Run black
        if: ${{ always() && steps.install-deps.outcome == 'success' }}
        run: |
          black --check flogin examples tests

  ruff:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ${{ toJson(vars.PYTHON_VERSIONS) }}

    name: Ruff on ${{ matrix.python-version }}
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0

      - name: Set up CPython ${{ matrix.python-version }}
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        id: install-deps
        run: |
          pip install .[tests]
          pip install ruff
      
      - name: Run Ruff Formatter
        if: ${{ always() && steps.install-deps.outcome == 'success' }}
        run: |
            ruff format --check